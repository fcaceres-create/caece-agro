# Dependencias del proyecto AgroSmart
# Versiones flexibles pero compatibles entre sí (Python 3.10+).

# Manipulación y análisis de datos
pandas>=2.0,<3.0
numpy>=1.24,<2.2

# Cliente HTTP para consumir APIs externas
requests>=2.31,<3.0

# Modelado estadístico y ML
scikit-learn>=1.3,<2.0
joblib>=1.3,<2.0

# Visualización
matplotlib>=3.7,<4.0
seaborn>=0.12,<1.0

# Notebooks
jupyter>=1.0,<2.0

# Barra de progreso para loops largos (consolidación de dataset maestro)
tqdm>=4.65,<5.0

# Puente Python <-> SWI-Prolog
pyswip>=0.2.10,<1.0

# Tests
pytest>=7.4,<9.0

# App web (Fase IV — interfaz de defensa oral)
streamlit>=1.32,<2.0
folium>=0.17,<1.0
streamlit-folium>=0.20,<1.0
plotly>=5.20,<7.0

# Generación del informe TFI académico (.docx)
python-docx>=1.1,<2.0
